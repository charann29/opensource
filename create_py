def find_max(a, b, c, d):
    max_value = a
    if b > max_value:
        max_value = b
    if c > max_value:
        max_value = c
    if d > max_value:
        max_value = d 
    return  max_value
result = find_max(5, 9, 3, 7)
print("maxmium:", result)
